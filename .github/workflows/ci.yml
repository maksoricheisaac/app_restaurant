name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

env:
  PNPM_VERSION: "9"
  NODE_VERSION: "22"

jobs:
  # ─── Secrets scan ─────────────────────────────────────────────────────────
  secrets:
    name: Secrets — Gitleaks scan
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Run Gitleaks
        uses: gitleaks/gitleaks-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITLEAKS_CONFIG: .gitleaks.toml

  # ─── Backend ──────────────────────────────────────────────────────────────
  backend:
    name: Backend — Lint / Typecheck / Build
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/backend

    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v3
        with:
          version: ${{ env.PNPM_VERSION }}

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm
          cache-dependency-path: apps/backend/pnpm-lock.yaml

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Audit dependencies (high/critical only)
        run: pnpm audit --prod --audit-level=high
        continue-on-error: true

      - name: Generate Prisma client
        run: pnpm exec prisma generate
        env:
          DATABASE_URL: postgresql://user:pass@localhost:5432/test

      - name: Validate Prisma schema
        run: pnpm exec prisma validate

      - name: Lint (check only — CI must not silently auto-fix and pass)
        run: pnpm lint:ci

      - name: Typecheck
        run: pnpm exec tsc --noEmit

      - name: Run unit tests with coverage (threshold 55%/45%/40%/55% — statements/branches/functions/lines, see jest.coverageThreshold in package.json)
        run: pnpm test:cov
        env:
          JWT_SECRET: ci_test_secret_min_32_chars_long_abc
          DATABASE_URL: postgresql://user:pass@localhost:5432/test
          FRONTEND_URL: http://localhost:4000

      - name: Run integration tests
        run: pnpm test:integration
        env:
          JWT_SECRET: ci_test_secret_min_32_chars_long_abc
          DATABASE_URL: postgresql://user:pass@localhost:5432/test
          FRONTEND_URL: http://localhost:4000

      - name: Build
        run: pnpm build
        env:
          JWT_SECRET: ci_test_secret_min_32_chars_long_abc
          DATABASE_URL: postgresql://user:pass@localhost:5432/test
          FRONTEND_URL: http://localhost:4000

  # ─── Frontend ─────────────────────────────────────────────────────────────
  frontend:
    name: Frontend — Lint / Typecheck / Build
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/frontend

    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v3
        with:
          version: ${{ env.PNPM_VERSION }}

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm
          cache-dependency-path: apps/frontend/pnpm-lock.yaml

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Audit dependencies (high/critical only)
        run: pnpm audit --prod --audit-level=high
        continue-on-error: true

      - name: Lint (check only — CI must not silently auto-fix and pass)
        run: pnpm lint:ci

      - name: Typecheck
        run: pnpm exec tsc --noEmit

      - name: Build
        run: pnpm build
        env:
          NEXT_PUBLIC_API_URL: http://localhost:3000/api/v1
          NEXT_PUBLIC_APP_URL: http://localhost
          BACKEND_URL: http://localhost:3000/api/v1

  # ─── Docker ───────────────────────────────────────────────────────────────
  docker:
    name: Docker Build Check
    runs-on: ubuntu-latest
    needs: [backend, frontend]
    # Runs on every push AND every PR: a broken Dockerfile (e.g. invalid
    # COPY syntax) must fail CI before merge, not only after — it used to
    # be push-only "to save CI minutes", which let a build-breaking bug
    # reach main/develop undetected.

    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build backend image (no push)
        uses: docker/build-push-action@v5
        with:
          context: ./apps/backend
          push: false
          cache-from: type=gha
          cache-to: type=gha,mode=max
          tags: flash-menu-backend:ci

      - name: Build frontend image (no push)
        uses: docker/build-push-action@v5
        with:
          context: ./apps/frontend
          push: false
          cache-from: type=gha
          cache-to: type=gha,mode=max
          tags: flash-menu-frontend:ci
          build-args: |
            NEXT_PUBLIC_API_URL=http://localhost:3000/api/v1
            NEXT_PUBLIC_APP_URL=http://localhost

  # ─── Push to Registry (main only) ────────────────────────────────────────
  publish:
    name: Push Docker images
    runs-on: ubuntu-latest
    needs: [docker]
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    environment: production

    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: |
            ghcr.io/${{ github.repository }}/backend
            ghcr.io/${{ github.repository }}/frontend
          tags: |
            type=sha,prefix=
            type=raw,value=latest

      - name: Build and push backend
        uses: docker/build-push-action@v5
        with:
          context: ./apps/backend
          push: true
          cache-from: type=gha
          cache-to: type=gha,mode=max
          tags: ghcr.io/${{ github.repository }}/backend:${{ github.sha }},ghcr.io/${{ github.repository }}/backend:latest

      - name: Build and push frontend
        uses: docker/build-push-action@v5
        with:
          context: ./apps/frontend
          push: true
          cache-from: type=gha
          cache-to: type=gha,mode=max
          tags: ghcr.io/${{ github.repository }}/frontend:${{ github.sha }},ghcr.io/${{ github.repository }}/frontend:latest
          build-args: |
            NEXT_PUBLIC_API_URL=${{ secrets.NEXT_PUBLIC_API_URL }}
            NEXT_PUBLIC_APP_URL=${{ secrets.NEXT_PUBLIC_APP_URL }}
