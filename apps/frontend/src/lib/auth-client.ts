// Stub: this project uses AuthContext (AuthContext.tsx) for auth, not Better Auth.
// This module exists only to satisfy imports from legacy components.
export const authClient: { signOut?: (opts: any) => Promise<void> } | null = null;
